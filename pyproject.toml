[tool.poetry]
name = "tensorplus"
version = "0.0.1"
description = "A streamlined tensor library"
authors = []
license = "MIT"
readme = "README.md"
repository = "https://github.com/octakitten/tensorplus"

[tool.poetry.dependencies]
python = ">=3.12"

[tool.poetry.dev-dependencies]
pytest = ">=7.4.3"