name: Tensorplus Wheel Build
on:
  push:
    branches:
      - main
env: 
  GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

jobs:
  build:
    runs-on: [self-hosted, linux, x64]
    container:
      image: octakitten/tensorplus-v01
    steps:
      - run: gh release create --verify-tag latest /tensorplus/dist/*.whl
